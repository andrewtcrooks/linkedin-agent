
## Durable memory flush (2026-03-03 09:13 PT)
- Upgraded OpenClaw from 2026.2.26 to 2026.3.2 successfully; config validation passed (`openclaw config validate --json` valid=true).
- Secrets migration completed: `openclaw.json` now references `${TELEGRAM_BOT_TOKEN}`, `${BRAVE_SEARCH_API_KEY}`, `${OPENCLAW_GATEWAY_TOKEN}` from `/home/claw/.openclaw/.env`.
- Security hardening applied:
  - `/home/claw/.openclaw` permissions set to `700`.
  - `gateway.trustedProxies` configured for loopback (`127.0.0.1/32`, `::1/128`) for local Caddy reverse proxy.
- GitHub backup automation in place:
  - Scripts: `bin/gh-app-token`, `bin/ghx`, `bin/workspace-backup`.
  - Branch/remote standardized on `origin/main` (`andrewtcrooks/openclaw-rook`).
  - `workspace-backup` updated to non-interactive push using GitHub App token header.
- Cron jobs active and important:
  - Daily 8:00 AM PT AI functionality digest to Andrew.
  - Workspace backups (hourly meaningful + 12h snapshot).
  - Proactive memory capture loop had recurring `~` path bug; cron prompt was patched to absolute path-only (`/home/claw/.openclaw/workspace/...`) + fail-stop rule.
- Funnel work completed for Datanova:
  - Created local scorecard app at `http://192.168.1.200:8099/`.
  - Added branded theme aligned to DataNova site style.
  - Added backend submit endpoint (`funnel/server.py`) that appends submissions to `data/marketing/leads.csv`.
  - Created sales assets: `funnel/scorecard.md`, `funnel/call-script.md`, marketing scripts and starter content calendar.
- Fan control:
  - Installed Argon-style fan control stack (`argononed`), confirmed service active.
  - Current configured fan curve in `/etc/argononed.conf`: 40=0, 50=25, 60=50, 70=75, 80=100.
  - Full reboot executed on request after service-level restart.
- User preference updates added to AGENTS.md:
  - Prompt-injection defense rules.
  - Anti-loop rules (including max 5 consecutive tool calls before check-in).
  - Cron prompt rule: on failure, report and stop; no auto-retry.
