## 2026-03-09 — Durable updates

- Fixed QMD maintenance reliability:
  - Forced CPU mode for QMD health tasks (`QMD_FORCE_CPU=1`) to avoid CUDA build/probe failures on this host.
  - Repaired/advanced embeddings; QMD reached healthy embedded state after controlled passes.
- Hardened cache cleanup cron job (`Safe cache cleanup (7AM daily)`):
  - No longer wipes `/home/claw/.cache/qmd/models`.
  - Preserves QMD model files and only removes transient partial files (`*.ipull`).
- QMD local model cache now includes all three expected models:
  - `hf_ggml-org_embeddinggemma-300M-Q8_0.gguf`
  - `qwen3-reranker-0.6b-q8_0.gguf`
  - `qmd-query-expansion-1.7B-q4_k_m.gguf`
- Added QMD guardrail monitor cron (`QMD guardrail monitor (every 6h)`) with state file `memory/qmd-health-state.json`:
  - Alerts on pending embeddings >24h or vector count drop >=10%.
- ERPNext lead workflow updates:
  - Corrected CSV taxonomy: replaced `Accounting/Bookkeeping` with `Accounting`.
  - Mapped lead upload to ERPNext `industry` field (removed `market_segment` dependency).
  - Successfully imported first-third CSV batch: 33 created, 0 failed.
- Installed ClawHub `mem0` skill.
- Installed Cognee and completed local setup:
  - Added wrapper: `/home/claw/.openclaw/workspace/bin/cognee-local`.
  - Downloaded local Cognee LLM model: `Qwen2.5-1.5B-Instruct-Q4_K_M.gguf` (bartowski).
  - Patched Cognee LLM gateway to strip unsupported kwarg (`ontology_file_path`) for llama-cpp compatibility.
  - Validated Cognee end-to-end: add -> cognify -> search returned expected preference result.
  - Added persistent patch helper: `/home/claw/.openclaw/workspace/bin/patch-cognee-local` and auto-apply hook in `cognee-local`.
- Added memory routing doc: `/home/claw/.openclaw/workspace/docs/memory-routing-policy.md` (QMD first, mem0 second, Cognee third).
- Installed Ollama locally (CPU mode) so mem0 can run without cloud API keys.

